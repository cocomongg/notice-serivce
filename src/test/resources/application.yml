spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always

file:
  upload:
    tmp-dir: uploads/tmp
    notice-dir: uploads/notice
    max-size: 10485760 # 10MB
    allowed-mime-types:
      - image/png
      - image/jpeg
      - application/pdf